<!--music.wxml--> 
<view class="root">
  <view class="title hl80"><text class="breadcrumbs">网易云音乐</text></view>
  <view class="swiper-tab hl80">  
    <view class="swiper-tab-list {{currentTab==0 ? 'hover' : ''}}" data-current="0" bindtap="switchTab">个性推荐</view>  
    <view class="swiper-tab-list {{currentTab==1 ? 'hover' : ''}}" data-current="1" bindtap="switchTab">歌单</view>  
    <view class="swiper-tab-list {{currentTab==2 ? 'hover' : ''}}" data-current="2" bindtap="switchTab">主播电台</view>
    <view class="swiper-tab-list {{currentTab==3 ? 'hover' : ''}}" data-current="3" bindtap="switchTab">排行榜</view>
  </view>
  <!--<swiper current="{{currentTab}}" duration="300" style="height:800rpx" bindchange="bindChange">
    <swiper-item>
      <view class="recommend-song-lists">
        <text class="hl80">推荐歌单</text>
        <view class="song-lists">
          <view class="song-list body-view" wx:key="{{item.id}}" wx:for="{{recommendSongList}}">
            <image src="{{item.picUrl}}"></image>  
            <text>{{item.name}}</text>
          </view>
        </view>
      </view>
    </swiper-item>
    <swiper-item>我是歌单~~</swiper-item>
    <swiper-item>我是主播啦~~</swiper-item>
    <swiper-item>我是至尊排行榜~~</swiper-item>
  </swiper> -->
  <view>
    <image src="./11.png"></image>  
    <text>gasgagas广大金刚经几个卡口感噶阿卡gas宫颈癌客观世界阿嘎烧烤工具啊嘎嘎两三个</text>
  </view>
</view>
